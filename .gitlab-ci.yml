stages:
    - build
    - test
    - deploy
like_service_build:
    image:
        name: maven:3.6-jdk-11
    stage: build
    only:
        changes:
            - "code/service-likes/*"
    script: "mvn clean package -f code/service-likes/pom.xml"
    artifacts:
        paths:
            - code/service-likes/target/*.jar

like_service_test:
    image:
        name: maven:3.6-jdk-11
    stage: test
    only:
        changes:
            - "code/service-likes/*"
    script:
        - cd code/service-likes
        - mvn test
like-service-deploy:
  stage: deploy
  script:
    - docker build service-map -t "$CI_REGISTRY_IMAGE_PREFIX/service-map:$CI_COMMIT_REF_SLUG" -t "$CI_REGISTRY_IMAGE_PREFIX/service-likes:latest"
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY
    - docker push --all-tags "$CI_REGISTRY_IMAGE_PREFIX/service-map"
  only:
    - master
